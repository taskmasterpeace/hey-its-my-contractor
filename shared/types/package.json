{
  "name": "@contractor-platform/types",
  "version": "0.1.0",
  "description": "Shared TypeScript types for contractor platform",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
